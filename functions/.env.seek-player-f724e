# 此檔會被 commit:供 CI/CD(無本地 .env)部署時帶入 align_lyrics 的設定。
# 值非機密——aeneas-align 是 --no-allow-unauthenticated 的 Cloud Run 服務,
# 僅憑 URL 無法呼叫(需 ID token + run.invoker)。本地開發仍以 .env 覆寫優先。
# Firebase Functions 載入順序:.env → .env.<projectId>(後者覆寫前者)。
ALIGN_SERVICE_URL=https://aeneas-align-ltro7kk2pq-de.a.run.app
ALIGN_RATE_LIMIT_PER_DAY=20
